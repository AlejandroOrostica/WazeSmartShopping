import 'package:flutter/material.dart';

class Tiendas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Aqui Irian todas las tiendas"),
    );
  }
}
